import { Field, InternalFieldErrors } from '../types';
declare const _default: ({ _f: { ref, refs, required, maxLength, minLength, min, max, pattern, validate, name, value: inputValue, valueAsNumber, mount, }, }: Field, validateAllFieldCriteria: boolean) => Promise<InternalFieldErrors>;
export default _default;
