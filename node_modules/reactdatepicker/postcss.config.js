/**
 * Created by chenlin on 2017/8/10 0010.
 */
module.exports = {
    plugins: [
        require('autoprefixer')({
            browsers:
                ["Android >= 4", "iOS >= 6"]
            // ['last 2 versions', 'Android >= 4.0','> 10%']
        })
    ]
}